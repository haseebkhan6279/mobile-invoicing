AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:5173"

# The standalone REST API (../api). No DB access from the dashboard anymore —
# everything goes through this API over HTTP.
API_URL="http://localhost:3000/api/v1"
